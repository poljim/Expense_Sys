
git checkout ss
git push -u origin master

> header (.h) files are for function, class etc. declarations.
> diff of struct and class: class' members are private, struct's are public by default 



Testing Requirements (from the document)
Provide at least 5 test cases including:
• Normal cases
• Edge cases (empty structure)
• Invalid input cases
• Duplicate record scenarios